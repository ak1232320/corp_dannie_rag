"""System-правила и пороги релевантности: ответ только по найденному контексту."""

SYSTEM_RULES = """
Ты отвечаешь только на основе найденных фрагментов из базы данных.
Не выдумывай факты, переменные, периоды и источники, которых нет в контексте.
Если релевантных фрагментов нет — явно откажись ответить.
""".strip()

# Промпт для LLM-генерации (улучшение 3). Контекст и вопрос подставляются.
LLM_PROMPT_TEMPLATE = """Ответь на вопрос пользователя СТРОГО на основе приведённых ниже фрагментов из базы клинических исследований (ClinicalTrials.gov).
Правила:
- Используй только информацию из фрагментов; не добавляй внешних знаний.
- Если во фрагментах нет ответа — честно скажи, что данных недостаточно.
- Отвечай кратко и по делу, на языке вопроса.
- В конце перечисли использованные источники как [номер] с их doc_id.

Фрагменты:
{context}

Вопрос: {question}

Ответ:"""

REFUSAL_NO_CONTEXT = (
    "В базе не найдено релевантных фрагментов. "
    "Ответить по данным невозможно."
)

REFUSAL_EMPTY_QUESTION = "Задайте вопрос."

# Порог релевантности для чистого TF-IDF (как в эталоне) — «шумовой пол» лексики.
MIN_SCORE = 0.15
# Пороги для гибридного режима. На клиническом англоязычном корпусе лексические
# скоры офтопа высоки (общие слова how/do/home дают TF-IDF до ~0.38), поэтому
# отказ в гибриде опирается на СЕМАНТИКУ: чанк релевантен, если семантический
# косинус >= SEM_MIN_SCORE ИЛИ лексический достаточно силён (>= LEX_STRONG_SCORE).
# (офтоп: sem<=0.37, lex<=0.38; домен: sem>=0.58, lex до 0.56 — порог 0.45 разделяет).
SEM_MIN_SCORE = 0.45
LEX_STRONG_SCORE = 0.45
